[package]
name = "budgets"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "2.0.20"
uuid = { version = "1", features = ["v7"] }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
